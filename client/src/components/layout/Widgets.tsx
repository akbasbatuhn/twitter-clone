import React from "react";

const Widgets = () => {
    return (
        <aside className="min-h-screen w-80 bg-green-200">
            <h1>Widgets</h1>
        </aside>
    );
};

export default Widgets;
