export type UserRegister = {
    userName: string;
    name: string;
    password: string;
    email: string;
};

export type User = {
    id: number;
    userName: string;
    name: string;
    bio: string;
    createdAt: string;
};
